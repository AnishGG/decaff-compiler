class Program{
    void main(){
        int sum, n;
        sum = 0;
        n = 9;
        sum = n * (n + 1);
        sum = sum / 2;
        callout("printf", "Sum of first %d numbers: %d\n", n, sum);
    }
}
