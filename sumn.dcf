class Program{

    int main(){
        int sum, n;
        sum = 0;
        // sum of n elements is n*(n + 1)/2
        callout(scanf, "%d", &n);
        sum = n * (n + 1);
        sum = sum / 2;
        callout(printf, "sum: %d\n", sum);
    }
}
